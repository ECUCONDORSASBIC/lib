/**
 * Basic test file to ensure Jest setup is working
 */

describe('Basic test suite', () => {
  test('1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2);
  });

  test('true is truthy', () => {
    expect(true).toBeTruthy();
  });
});
